services:
  answer:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        GOPROXY: ${OPENCLASS_ANSWER_GOPROXY:-https://proxy.golang.org,direct}
        NPM_REGISTRY: ${OPENCLASS_ANSWER_NPM_REGISTRY:-https://registry.npmjs.org}
        BASE_PATH: ${OPENCLASS_ANSWER_BASE_PATH:-}
        NODE_OPTIONS: ${OPENCLASS_ANSWER_NODE_OPTIONS:---max-old-space-size=1536}
    image: openclass-answer:2.0.2-connector-basic-1.2.12
    restart: unless-stopped
    ports:
      - "127.0.0.1:${OPENCLASS_ANSWER_PORT:-9080}:80"
    environment:
      TIMEZONE: ${TZ:-Asia/Shanghai}
      AUTO_INSTALL: ${OPENCLASS_ANSWER_AUTO_INSTALL:-}
      DB_TYPE: sqlite3
      DB_FILE: /data/answer.db
      LANGUAGE: ${OPENCLASS_ANSWER_LANGUAGE:-zh_CN}
      SITE_NAME: ${OPENCLASS_ANSWER_SITE_NAME:-OpenClass 学习社区}
      SITE_URL: ${OPENCLASS_ANSWER_SITE_URL:-http://127.0.0.1:9080}
      OPENCLASS_ANSWER_THEME_ENABLED: ${OPENCLASS_ANSWER_THEME_ENABLED:-true}
      OPENCLASS_ANSWER_MIN_TAGS: ${OPENCLASS_ANSWER_MIN_TAGS:-0}
      OPENCLASS_HOME_URL: ${OPENCLASS_HOME_URL:-http://127.0.0.1:3000/home}
      OPENCLASS_FAVICON_URL: ${OPENCLASS_FAVICON_URL:-}
      OPENCLASS_COMMUNITY_PUBLIC_URL: ${OPENCLASS_COMMUNITY_PUBLIC_URL:-}
      OPENCLASS_COMMUNITY_OAUTH_CLIENT_ID: ${OPENCLASS_COMMUNITY_OAUTH_CLIENT_ID:-}
      OPENCLASS_COMMUNITY_OAUTH_CLIENT_SECRET: ${OPENCLASS_COMMUNITY_OAUTH_CLIENT_SECRET:-}
      OPENCLASS_COMMUNITY_OAUTH_AUTHORIZE_URL: ${OPENCLASS_COMMUNITY_OAUTH_AUTHORIZE_URL:-}
      OPENCLASS_COMMUNITY_OAUTH_TOKEN_URL: ${OPENCLASS_COMMUNITY_OAUTH_TOKEN_URL:-}
      OPENCLASS_COMMUNITY_OAUTH_USERINFO_URL: ${OPENCLASS_COMMUNITY_OAUTH_USERINFO_URL:-}
      CONTACT_EMAIL: ${OPENCLASS_ANSWER_CONTACT_EMAIL:-}
      ADMIN_NAME: ${OPENCLASS_ANSWER_BOOTSTRAP_ADMIN_NAME:-openclassadmin}
      ADMIN_PASSWORD: ${OPENCLASS_ANSWER_BOOTSTRAP_ADMIN_PASSWORD:-}
      ADMIN_EMAIL: ${OPENCLASS_ANSWER_BOOTSTRAP_ADMIN_EMAIL:-answer-admin@openclass.local}
      EXTERNAL_CONTENT_DISPLAY: ask_before_display
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - answer-data:/data
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1/answer/api/v1/siteinfo"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  answer-data:
